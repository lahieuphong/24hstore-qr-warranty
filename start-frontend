#!/usr/bin/env bash

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
echo "Trang tra cứu đã được tích hợp vào backend: http://127.0.0.1:8000/check"
echo "Đang khởi động backend trên cổng 8000..."
echo

exec "$SCRIPT_DIR/start-backend"
